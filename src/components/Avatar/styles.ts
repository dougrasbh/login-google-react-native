import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 170,
    borderRadius: 30,
    justifyContent: 'center',
    alignItems: 'center',
  },
  image: {
    width: 170,
    height: 180,
    borderRadius: 25,
    borderWidth: 7,
    borderColor: '#B6D96A'
  },
});